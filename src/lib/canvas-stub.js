// Empty stub — pdfjs-dist optionally requires canvas in Node.js environments.
// In the browser build (Turbopack / webpack) we stub it out so the import resolves
// to an empty module without errors.
module.exports = {};
